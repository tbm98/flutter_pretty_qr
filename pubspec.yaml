name: pretty_qr_code
description: Pretty QR code for Flutter. You can round the edges with parameter or use the standard view.
version: 1.0.1
author: Oleg Lobkov <flip.oleg@gmail.com>
homepage: https://github.com/promops/flutter_pretty_qr

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  qr: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
